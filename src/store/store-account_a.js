import { reactive } from 'vue'
import { User } from '../types/User'

const state = reactive({
    user: User,
    token: '',
    authenticated: false,
    access: User.
});

export default {
    state
}
